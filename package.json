{
  "name": "foodswap-backend",
  "version": "1.0.0",
  "private": true,
  "description": "App-owned transcription (Whisper) + vision proxy for Food Swap. Keeps the OpenAI key server-side.",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
